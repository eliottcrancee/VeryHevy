# Normalise les fins de ligne : LF dans le dépôt et dans l'espace de travail,
# quel que soit le système (Windows local, CI Linux).
* text=auto eol=lf

# Fichiers binaires
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary